# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Data cache
data_cache/

# Strategy implementation files
# NOTE: Strategy modules are part of the deployed backend and must be tracked.
# If you have local-only experiments, put them under a separate ignored directory
# (e.g. `core/strategy/local/`) rather than ignoring real modules.
#
# Local-only strategy experiments (keep out of deploys)
core/strategy/leveraged_etf_vol_swing.py
core/strategy/mystic_pulse.py
core/strategy/wheel.py

# Local databases
*.db

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Environment and secrets
.env
*.env
.env.local
.env.*.local

# Secrets files - contains actual API tokens - never commit these!
config/*.secret.yaml
config/*.local.yaml
config/secrets.yaml
/secrets.yaml
**/secrets.yaml
**/*secrets*.yaml
**/*secrets*.yml

# API keys and credentials
**/*.key
**/*.pem
**/*credentials*.yaml
**/*credentials*.yml
**/*credentials*.json
**/*api_key*
**/*api_token*
**/*secret_key*
**/*access_token*

# Local overrides (may contain secrets)
config/*.local.*
*.local.*

