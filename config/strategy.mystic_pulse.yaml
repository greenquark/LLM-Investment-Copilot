# Note: symbol is configured in env.backtest.yaml, not here
adx_length: 9
smoothing_factor: 1
collect_length: 100
contrast_gamma_bars: 0.7
contrast_gamma_plots: 0.8
min_trend_score: 5
timeframe: 1D  # Allowed timeframes: 15m, 30m, 1H, 2H, 4H, 1D
capital_deployment_pct: 1.0  # Percentage of available capital to deploy per trade (0.0 to 1.0). 1.0 = 100% for testing

